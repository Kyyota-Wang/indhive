# Data References

This POC uses fictional synthetic records only.

`data/source_cases` contains agent-visible synthetic source records that mimic source documents already extracted into structured snippets. These are not expected canonical answers.

`data/evaluation/golden_truth` contains evaluation-only expected outputs for selected golden cases. Application code and the Module 1 agent pipeline must not read these files during generation.

