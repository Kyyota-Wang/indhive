# INDHIVE v1.1 — 实现规格

写给下一个会话的执行文档。上一轮的分析和决策已经做完，**不要重新论证**，照做即可。

日期：2026-08-23 · 目标：当晚与临床搭档（fenglpku2000）的评审会

---

## 0. 三十秒背景

INDHIVE 是 FDA IND **Module 1** 自动化准备的演示站，已上线 https://indhive.com。

```
indkit/     Python 流水线 —— 全部生成逻辑的唯一实现
        │  python scripts/run_pipeline.py --all
        │  python build/bundle_cases.py   →  V1/src/cases.json
V1/                Cloudflare Worker（前端 + /api/chat，助手叫 Iris）
```

部署、回滚、验证清单见 `DEPLOY.md`。**不要改动部署配置。**

现状：10 个虚构案例（IND001–IND010），六个视图（1 个 Input + 5 个生成产物）。

---

## 1. 这一版要解决的问题

搭档反馈的核心：**虚构输入 → 虚构输出，他没法 review**。

他在共享盘里造了一套真实形状的输入（PMX-103 项目），已拉到本地：

```
inputs/partner-package/AI Inputs/
├── 06_Form_FDA_1571_AutoFilled.docx      他手填的 1571（= 标准答案）
├── 07_CMC_Dossier_PMX103_v3.docx         312 KB
├── 08_Investigator_Brochure_PMX103_v3.docx
├── 09_Nonclinical_Development_Plan_PMX103_v3.docx
├── 10_Phase1_Protocol_PMX103_P1_001_v3.docx
├── 11_Toxicology_Dossier_PMX103_v3.docx
├── 12_IND_Drafting_Input_Brief_PMX103.docx   主输入，Field|Value|Source 三列
├── 13_IND_Drafting_Instructions_PMX103.docx  给 agent 的任务书
├── 14_IND_Section_Traceability_Matrix.xlsx   ★ Section Map / GAPS Log / Invariants
└── 15_AI_Agent_Input_Schema.json             ★ 机器可读，直接用这个做映射
```

**`15_..._Schema.json` 和 `14_...xlsx` 是主要数据源**，两份 docx 规格（12/13）读一遍理解意图即可，五份大 dossier 只在做不变量扫描时当语料。

---

## 2. 已定的决策（不要推翻）

1. **绝不扩到 M1–M5。** 搭档的 `13 §1` 要求 eCTD M1–M5，用户明确否决。本轮只做 M1。
2. **PMX-103 单独成区**，不与 IND001–010 并排。它性质不同：真实输入、有标准答案、可被评审。首页给它独立的一块，标 `Partner input · reviewable`。
3. **主动挑明"薄"。** 我们 canonical schema 只有约 35 个标量字段，他包里有 PK 表、10 个剂量水平、6 项毒理研究、9 条 CMC 规格。绝大部分无处安放。**必须在界面上写明**：Module 1 只需要这些，其余属于 M2–M5，未建也不打算现在建。把"薄"讲成"边界清晰"，不要等他发现。
4. **不新增虚构案例。** 第 11 个虚构案例没有价值。

---

## 3. 四项任务（按优先级，可随时停止）

### ① PMX-103 上站 —— 必做

把 `15_AI_Agent_Input_Schema.json` 映射进现有 canonical schema，作为一个新 case 走完整条流水线。

实测过的映射（`program.*` 直接可用）：

| canonical | 来源 | 值 |
|---|---|---|
| `sponsor.legal_name` | `program.sponsor` | Indela Therapeutics, Inc. |
| `sponsor.address_line_1` / city / state / postal | `program.sponsor_addr` | 1515 Hollins Street, Suite 400, Baltimore, MD 21202 |
| `sponsor.contact_name` / `contact_title` | `authors[]` 中的 Laura Whitfield（签署人） | VP, Regulatory Affairs |
| `sponsor.phone` | `06_Form_1571` 第 4 项 | +1 410 555 0143 |
| `product.code_name` | `program.drug_code` | PMX-103 |
| `product.generic_name` | `program.drug_full_name` | anti-PRAME/HLA-A*02:01 × anti-CD3ε |
| `product.indication` | `program.indication` | — |
| `protocol.protocol_number` | 协议文件 | PMX-103-P1-001 |
| `protocol.phase` | `program.phase` | Phase 1 |
| `protocol.version` / `protocol_date` | dossier v3.0 / 22 Aug 2026 | — |
| `submission.submission_type` | — | Initial IND |
| `submission.submission_date` | `program.submit_date` | 21 August 2026 |
| `submission.serial_number` | — | 0000 |
| `submission.ind_number` | **留空** | → GAP-01 |
| `investigator.*` | **全部留空** | → GAP-04 |
| `plan.*`（1.20 六要素） | `program.starting_dose_rationale`、`dose_design`、safety 值等 | 可填满 |

**关键：`sponsor.email` 他没给，`investigator.*` 整段没有。这些必须留空，让流水线自己标出来** —— 见任务 ②。

源记录要拆成多条（sponsor_profile / product_brief / protocol_synopsis / submission_intake / investigational_plan），沿用现有 `source_records` 结构，`source_type` 用 `partner_supplied_structured_data` 以区别于 `synthetic_structured_data`。

### ② 缺口分析 vs 他的 GAPS Log 对照 —— 必做

**这是全场最强的一点。** 我们的确定性代码应该能独立复现他手工整理的缺口清单。已核对过的对应关系：

| 我们会输出 | 他的 GAPS Log |
|---|---|
| Box 6 IND Number 空 / WARNING | **GAP-01** 实际 IND 号未分配 |
| `investigator.*` 缺失 → 1.1.2 Form 1572 `ABSENT` | **GAP-04** 站点和研究者名单未定 |
| 1.1.3 Form 3674 `ABSENT` | **GAP-05** NCT ID 未注册 |
| 1.3.4 财务披露 `NEEDS DECISION` | **GAP-06** 3454/3455 待逐研究者收集 |

做一个并排对照视图：左边我们推导出的缺口，右边他 `14 › GAPS Log` 的条目，中间标 ✓ 一致 / ⚠ 只有我们发现 / ⚠ 只有他列出。

**同时把 owner 和解锁条件加进我们的缺口模型**（`owner`、`source_needed`、`status`），这是他 `13 §7` 交付物第 2 条明确要的。

### ③ 1571 与他手填版逐字段 diff —— 争取

`06_Form_FDA_1571_AutoFilled.docx` 是标准答案。逐字段比对我们生成的 1571。

**⚠ 已知陷阱：他的字段编号和真实 Form 1571 对不上。** 他编了 1–17，真表是 Box 1–13 加签名区。已确认的错位：

| 他的编号 | 真实 box |
|---|---|
| 2. Address of Sponsor | Box **3**（Box 2 是 Date of Submission） |
| 6. Indication(s) | Box **7** |
| 7. Phase of Study | Box **8** |
| 9. Type of submission | Box **11** |
| 10. Contents of Application "Section a–k" | Box **12**，而且是 **1–10 项**不是 a–k |
| 3. Representative / 8 / 11 / 12 | 真表没有这些编号 |

我们上一轮已经把 box 映射对齐到真表（`indkit/src/indkit/pipeline/form_1571.py` 里的 `BOX_NUMBERS` / `BOX_LABELS`），**我们的比他准**。diff 视图要按真实 box 号组织，并明确标出"他这份草稿的编号与官方表单不一致"——这是要反馈给他的第一条。

### ④ 23 个不变量跨文档一致性报告 —— 看时间

`14 › Invariants` 有 23 行（Parameter / Value / Unit / Source）。语料是五份 dossier + brief，共 **725,863 字符**。

**⚠ 已实测：朴素正则不可用。** 锚定参数名后取邻近数字会产生大量假阳性：

```
Starting dose  →  抓到 72309    ← URL  fda.gov/media/72309/download
MTD (mouse)    →  抓到 103      ← 药物代号 PMX-103
MTD (mouse)    →  抓到 001      ← 研究编号 PMX-103-P1-001
NOAEL (cyno)   →  抓到 50 ug/kg ← 同句里的 HNSTD，不是 NOAEL
```

**必须用四步法：**

```
① 确定性定位   找参数名每次出现，抽出完整句子 → 候选断言集合
② LLM 判定     逐句问"这句是否在断言 <参数> = <值> <单位>?"
                只做判别不做生成，URL/代号里的数字在这步被判掉
③ 确定性比对   与 Invariants 表比对，单位归一化（microgram/µg/mcg/ug）
④ 证据留存     每条断言带 文档名 + 原句 + 字符位置，人点一下能自己核实
```

**⚠ 基线是干净的。** 已抽查 NOAEL / MABEL / 起始剂量 / EC50，六份文档全部一致。所以报告会全绿。

全绿本身是有价值的结论（"你们 v3.0 在 23 个不变量上完全自洽，这是证据"），但**要有冲击力得配现场篡改演示**：当场改 IB 里一个数字 → 重跑 → 系统指出文档 + 原句。准备一个脚本做这件事。

---

## 4. 要反馈给搭档的两处错误

会上说出来，价值高于任何功能演示。

**a) 他的 1571 字段编号与官方表单不符** —— 见任务 ③ 的表。

**b) 他自己两份文件互相矛盾：**

- `14 › Section Map` 写 (a)(7) CMC → `M1.7 / M3`，(a)(8) → `M1.8 / M2.6 / M4`
- `12 › Section C` 正确地写 CMC → Module 3，非临床 → Module 4

`M1.7` / `M1.8` 不是真实 eCTD 位置。那一列把 21 CFR 312.23(a)(x) 的法条编号和 eCTD 模块编号混在了一起。

**这正是我们这个工具该抓的那类跨文档不一致** —— 用他自己的材料证明工具的价值。

---

## 5. 会改到的文件

| 文件 | 改动 |
|---|---|
| `indkit/data/source_cases/PMX103.json` | 新增，从 `15_..._Schema.json` 映射 |
| `indkit/src/indkit/pipeline/toc.py` | 缺口模型加 `owner` / `source_needed` / `status` |
| `indkit/src/indkit/pipeline/` | 可能新增 `invariants.py`（任务 ④） |
| `V1/build/bundle_cases.py` | 带上新字段 |
| `V1/public/app.js` / `index.html` / `styles.css` | PMX-103 独立区 + 对照视图 |
| `V1/src/tools.ts` / `prompt.ts` | Iris 要能讲这个 case 和缺口对照 |

改完 Python 侧必须重跑：

```bash
cd indkit && python scripts/run_pipeline.py --all
cd ../V1 && python build/bundle_cases.py
```

---

## 6. 验证

1. `cd V1 && npx tsc --noEmit` 干净
2. PMX-103 出现在首页独立区，标注 `Partner input · reviewable`
3. 它的 1571 里 Box 6 空、`investigator.*` 缺失 —— 与 GAP-01 / GAP-04 对应
4. 缺口对照视图里四条对应关系全部标 ✓
5. "薄"的边界声明在界面上可见
6. Iris 被问到 PMX-103 时能讲清缺口对照，且**仍然拒绝**替人解决冲突
7. 浏览器控制台干净
8. 部署后在 https://indhive.com 复验 2–6

**Windows 提醒**：`Ctrl+C` 杀不干净 `workerd`，改了没生效时先清僵尸进程，命令见 `DEPLOY.md` §8.2。

---

## 7. 不要做

- 不要扩到 Module 2–5
- 不要新增虚构案例
- 不要动部署配置、域名、密钥
- 不要改「冲突绝不自动选边」这条核心规则
- 不要把搭档材料里的真实人名当作可公开数据处理之外的用途；站点本身是 public 仓库，PMX-103 是搭档造的虚构项目，可以上站
